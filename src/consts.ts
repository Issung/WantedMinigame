export const WIDTH = 256;
export const HEIGHT = 192;
export const HEAD_WIDTH = 30;
export const HEAD_HEIGHT = 32;
export const MIN_SPEED = 10;
export const MAX_SPEED = 45;
export const HEAD_COUNT = 64;

/** Not the true center, but where you need to put a sprite for it to be centered. */
export const CENTER_X = (WIDTH / 2) - (HEAD_WIDTH / 2);
export const CENTER_Y = (HEIGHT / 2) - (HEAD_HEIGHT / 2);